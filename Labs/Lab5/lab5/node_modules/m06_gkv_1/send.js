const nodemailer = require('nodemailer');

const transporter = nodemailer.createTransport({
    host: 'smtp.gmail.com',
    port: 465,
    auth: {
        user: 'kirillgvozd0@gmail.com',
        pass: 'qqhu bkrt hzvo exkp'
    }
});

function send(mail) {
    const option = {
        from: "kirillgvozd0@gmail.com",
        to: "kirillgvozd007@gmail.com",
        subject: "Send function",
        text: mail,
    };

    transporter.sendMail(option, (err, info) => {
        if (err) {
            console.log(err);
        } else {
            console.log(info);
        }
    });
}

module.exports = send;